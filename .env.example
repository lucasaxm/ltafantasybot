# =============================================================================
# LTA Fantasy Bot Configuration Template
# Copy this file to .env and fill in your values
# =============================================================================

# Required: Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here
ALLOWED_USER_ID=your_telegram_user_id

# Required: LTA Fantasy API Authentication  
X_SESSION_TOKEN=your_lta_fantasy_session_token

# Optional: Bot Behavior Settings
POLL_SECS=30  # Legacy: kept for backward compatibility - now uses phase-specific polling
PRE_MARKET_POLL_SECS=900  # 15 minutes polling during PRE_MARKET phase
MARKET_OPEN_POLL_SECS=60  # 1 minute polling during MARKET_OPEN phase  
LIVE_POLL_SECS=30         # 30 seconds polling during LIVE phase
LOG_LEVEL=INFO

# Optional: Watcher Behavior Settings
STALE_THRESHOLD=5    # Number of consecutive unchanged polls before triggering backoff
BACKOFF_MULTIPLIER=2  # Multiply current interval by this factor during backoff (max 4x)

# Optional: API Endpoint Configuration
# Set to direct API URL or your Cloudflare Worker URL
# Default: https://api.ltafantasy.com (direct API)
# For VPS with Cloudflare challenges: https://your-worker.workers.dev
LTA_API_URL=https://api.ltafantasy.com

# Optional: Cloudflare Worker Deployment (using Wrangler CLI)
# No additional environment variables needed - Wrangler handles authentication

# =============================================================================
# How to get these values:
#
# BOT_TOKEN: 
#   1. Message @BotFather on Telegram
#   2. Create a new bot with /newbot
#   3. Copy the token
#
# ALLOWED_USER_ID:
#   1. Message @userinfobot on Telegram
#   2. Copy your user ID number
#
# X_SESSION_TOKEN:
#   1. Log into https://ltafantasy.com
#   2. Open Developer Tools (F12)
#   3. Go to Network tab, refresh page
#   4. Find request to api.ltafantasy.com
#   5. Copy the x-session-token header value
#
# CLOUDFLARE_WORKER_URL:
#   1. Deploy worker.js using: npm run deploy
#   2. Copy the worker URL and set it as LTA_API_URL
# =============================================================================
