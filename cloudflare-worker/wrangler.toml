name = "cblol-fantasy-proxy"
main = "worker.js"
compatibility_date = "2023-05-18"

# Cloudflare Worker configuration for CBLOL Fantasy Bot proxy
# This worker bypasses Cloudflare challenges for VPS deployments

[env.production]
# Add any environment variables here if needed
# The worker automatically forwards x-session-token headers from requests
